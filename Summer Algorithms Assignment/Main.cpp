#include "IO_Manager.h"

// Main function
// The Assignment is to create a program that gets a non directed graph from the user
// and then finds the bridges in the graph - if there are any.
int main()
{
	io_manager::run_program();
	return 0;
}